# Release Link Role Notes

The source link identifies the dataset directly consumed as input. The model link identifies the model used to synthesize or transform records.

For the primary card, The Stack Dedup is the source and Qwen2.5-Coder is the synthesis model.
For the secondary card, UltraChat 200k is the source and DeepSeek-Coder-V2-Lite-Instruct is the synthesis model.
